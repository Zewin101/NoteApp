import 'package:flutter/material.dart';

class testddd extends StatelessWidget {

static const routeName='ffd';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
